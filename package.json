{
  "name": "postman_newman_jenkins",
  "version": "1.0.0",
  "description": "Project for storing postman tests",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-test": "newman run tests/testsuite1.postman_collection.json -r 'cli, junit' --reporter-junit-export newman.xml"
  },
  "author": "Karthikeyan Mariappan",
 "dependencies": {
    "newman": "^4.5.7"
  }
}
